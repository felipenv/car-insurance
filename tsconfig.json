{
  // Solution-style root config: `tsc -b` builds every referenced package in
  // dependency order (CORE before WEB, since WEB references CORE).
  "files": [],
  "references": [{ "path": "packages/core" }, { "path": "packages/web" }]
}
